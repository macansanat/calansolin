# قواعدِ کار روی این پروژه (calansolin)

## ۰) ⛔ قانونِ زبان — مهم‌ترین و بدونِ استثنا
**همهٔ متنی که به کاربر نشان داده می‌شود باید فارسی باشد. تحتِ هر شرایطی.**

این یعنی «همه‌چیز»، نه فقط پاسخِ نهایی:
- جمله‌های کوتاهِ توضیحِ پیش از هر ابزار (preamble/narration) — مثلِ «حالا این فایل را می‌خوانم…»،
  «بگذار کد را بررسی کنم…»، «ریشهٔ باگ را پیدا کردم…» — **باید فارسی باشند**.
- عنوانِ کارها، خلاصهٔ کار، گزارشِ نتیجه، سؤال از کاربر، پیشنهادها، و هر جملهٔ دیگر.
- حتی **یک جملهٔ** انگلیسی هم نقضِ این قانون است.

استثنا فقط و فقط وقتی است که **خودِ کاربر صریحاً** بخواهد انگلیسی بنویسم.

نام‌های فنی (نامِ تابع، کلاس، فایل، دستورِ گیت، کدها) طبیعتاً لاتین می‌مانند —
منظور از این قانون، **جمله‌ها و متنِ توضیحی** است.

> پیشینه: کاربر دو بار با اسکرین‌شات نشان داد که جمله‌های میانیِ من انگلیسی نوشته می‌شدند
> و از این بابت ناراضی بود. این اشتباه نباید دوباره تکرار شود.

## ۱) پاسخ‌ها کوتاه باشند
پاسخِ فارسیِ کوتاه و مفید؛ از توضیحِ اضافه و تکرار پرهیز کن.

## ۲) صداقتِ فنی
اگر کاری گران، نشدنی یا مصنوعی است، صادقانه بگو و بهترین جایگزینِ کم‌هزینه را پیشنهاد بده.
هرگز نتیجه‌ای را «انجام‌شده» اعلام نکن که تست نشده است.

## ۳) ⛔ قانونِ طلایی — کدِ قبلی را نگه دار، فقط ادیت کن
بازنویسیِ از صفر ممنوع. تغییرات باید **حداقلی و موضعی** باشند.
اگر فکر می‌کنی بازنویسیِ یک بخش لازم است، **اول بپرس**.

## ۴) مدیریتِ نسخه
هر تغییرِ قابلِ انتشار باید نسخه را بالا ببرد و **همان لحظه** این‌ها با هم به‌روز شوند:
۱) فوترِ داخلِ `index.html` (رشتهٔ «نسخه‌ی X · انتشار: تاریخ»)، ۲) سرتیترِ راهنما، ۳) تاریخچهٔ نسخه‌ها.
موقعِ پوش صریح بگو کدام نسخه منتشر شد.

## ۵) جریانِ گیت
توسعه روی شاخهٔ feature ← تستِ headless ← کامیت/پوش ← ادغامِ `--no-ff` در `main` ← بازگشت به شاخهٔ feature.
پیام‌های کامیت **فارسی**. شناسهٔ مدل را در هیچ آرتیفکتِ مخزن نگذار (فقط در چت).

## ۶) تستِ headless (Playwright)
کروم: `/opt/pw-browsers/chromium-*/chrome-linux/chrome` ·
اجرا با `NODE_PATH=/opt/node22/lib/node_modules /opt/node22/bin/node script.js`.
هدف: **صفر خطای صفحه**. برای تستِ اسکلتِ سه‌بعدی، `three` را محلی بگیر
(`npm pack three@0.128.0`) و با `addScriptTag` تزریق کن — CDN در محیطِ ساخت بسته است.

## ۷) نکتهٔ فنیِ فایل
`index.html` به‌خاطرِ ایموجی‌ها توسطِ ripgrep «binary» تشخیص داده می‌شود؛
برای جست‌وجو از `grep -an` استفاده کن. برای ویرایشِ یونیکدی از `python3` با heredoc و `str.replace` استفاده کن.

## ۸) خط‌قرمزهای ایمنی (اپِ دیابت)
برنامه **فقط آموزشی** است؛ هرگز دوز یا زمان‌بندیِ تزریقِ شخصی برای اجرای مستقیم نده.
دادهٔ نقطه‌ایِ گلوکومتر را هرگز به‌شکلِ TIR٪/GMI/AGP استاندارد نشان نده.
نام‌خانوادگی، ایمیل و موبایل هرگز به AI داده نمی‌شوند.
