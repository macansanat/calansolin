# راهنمای پروژه — دستیار آموزشی دیابت (Diabetes Educational Assistant)

> این فایل برای ادامه‌ی توسعه‌ی پروژه در **Claude Code** نوشته شده است.
> کل زمینه، تصمیم‌های کلیدی، ساختار فنی، و کارهای باقی‌مانده اینجا مستند شده.

---

## ۱. پروژه چیست؟

یک اپلیکیشن تک‌فایلی HTML (فارسی، RTL، تم تیره) برای **آموزش و مدیریت شخصی دیابت**.
کاربر هدف: یک فرد دیابتی نوع ۲ (۳۳ ساله، ۷۸ کیلو) که با انسولین اپیدرا (سریع) + توجئو (پایه) درمان می‌شود و از CGM استفاده می‌کند.

**فایل اصلی:** `insulin-calculator.html` (تک‌فایل، بدون بک‌اند، ~۲۳۰۰ خط)

فونت: Vazirmatn از CDN. کتابخانه: SheetJS (xlsx) از CDN برای خواندن اکسل.
ذخیره‌سازی: `localStorage` (و `window.storage` اگر در دسترس باشد) — روی هر دستگاه جدا.

---

## ۲. ⚠️ خط قرمز ایمنی — مهم‌ترین بخش این سند

این پروژه یک ابزار **آموزشی** است، نه یک دستگاه پزشکی. در طول کل توسعه، یک اصل بدون استثنا رعایت شده و **باید ادامه یابد**:

**نرم‌افزار هرگز نباید یک «زمان تزریق شخصی‌شده» یا «دوز قطعی» برای اجرای مستقیم تولید کند.**

تفکیک دقیق چیزی که مجاز است و چیزی که نیست:

| ✅ مجاز (آموزشی/تحلیلی) | ❌ ممنوع (تجویز) |
|---|---|
| نمایش فرمول‌های عمومی (۵۰۰÷TDD و...) | گفتن «تو X دقیقه قبل غذا بزن» |
| تخمین دوز از روی I:C که خود کاربر/پزشک وارد کرده | تولید خودکار زمان تزریق بر اساس قند لحظه‌ای فرد |
| اصول کلی زمان‌بندی («اپیدرا اوجش ۱-۱.۵ ساعت بعد») | تبدیل آن اصول به یک عدد عملیاتی برای یک فرد خاص |
| تحلیل **گذشته‌نگر** داده‌ها (کالیبراسیون از روی وعده‌های قبلی) | پیشنهاد دوز آینده به‌صورت خودکار |
| کشف I:C واقعی و **پیشنهاد** آن با تأیید دستی کاربر | اعمال خودکار I:C در محاسبه بدون تأیید کاربر |

**قاعده‌ی طلایی:** ابزار «آینه» نشان می‌دهد (تحلیل گذشته) و کاربر را به سمت **گفتگو با پزشک** هدایت می‌کند. هرچه ابزار کامل‌تر، واضح‌تر باید بگوید «این را با پزشکت چک کن». کاربر در طول توسعه چند بار خواست این خط را رد کنیم (با فشار، با ادعای تأیید پزشک، با «فقط برای اطلاعات»)؛ هر بار مؤدبانه رد شد. این تصمیم عمدی و درست است — حفظش کن.

نکته: کاربر خودش هم در نهایت به این فلسفه رسید و آن را پذیرفت: «هدف این است که فرد بیشتر حس کند به پزشک نیاز دارد و مستندسازی کند تا با پزشک در میان بگذارد.»

---

## ۳. ساختار فعلی — سه تب

### تب ۱: 🧮 ماشین‌حساب دوز
- **پروفایل و انسولین:** نوع دیابت (۷ گزینه، فقط ثبت می‌شود و روی محاسبه اثر ندارد — عمدی)، سن، وزن، انسولین سریع (۸ گزینه)، انسولین پایه (۱۱ گزینه شامل رایزودگ)، TDD، I:C، ISF قابل‌ویرایش. ذخیره در `localStorage`.
- **محاسبه‌ی کربوهیدرات وعده:** جستجو + انتخاب از دیتابیس ۵۷۴ خوراکی در ۲۹ دسته (~۱۲۴۶ واحد). هر خوراکی ایموجی هوشمند دارد. استپر +/− برای مقدار.
- **خلاصه‌ی وعده:** لیست غذاها با کربوهیدرات هرکدام (برای اسکرین‌شات).
- **ارزش تغذیه‌ای:** کربوهیدرات/پروتئین/چربی/کالری تخمینی + ارزیابی نسبت به وزن.
- **قند فعلی/هدف:** با استپر.
- **تخمین دوز:** دوز غذا (کرب÷I:C) + دوز اصلاحی ((فعلی−هدف)÷ISF). فقط آموزشی.
- **راهنمای زمان‌بندی پویا:** بر اساس نوع غذا و قند، اصول مرتبط را نشان می‌دهد — **بدون عدد تجویزی**.
- بخش آموزشی اصول زمان‌بندی (کشویی).

### تب ۲: 📅 برنامه‌ی هفتگی
- **تقویم شمسی (جلالی):** تبدیل با تابع `g2j()`. هفته از شنبه. ناوبری هفته قبل/بعد.
- **وارد کردن از فایل لاگ:** آپلود اکسل خروجی اپ CGM (شیت‌های Meal و Insulin) → نمایش پیش‌نمایش → کاربر انتخاب می‌کند → ثبت در برنامه.
- **فرم افزودن وعده:** نوع وعده، ساعت، جستجوی غذا از دیتابیس، انسولین، دوز، **کربوهیدرات، قند قبل، قند بعد، پیک، دره** (برای کالیبراسیون).
- **وعده‌های ثبت‌شده:** لیست هر روز، قابل حذف.
- **انسولین پایه‌ی روزانه:** جدا از وعده‌ها، بعد از لیست وعده‌ها.
- **کالیبراسیون I:C:** از وعده‌های «موفق» (قند بعد ۷۰-۱۸۰، پیک زیر ۱۸۰، دره بالای ۷۰) میانگین (کرب÷انسولین) را حساب می‌کند و I:C واقعی را نشان می‌دهد. دکمه‌ی «اعمال در پروفایل» (دستی). **هشدار مراجعه به پزشک** وقتی تفاوت I:C زیاد یا وعده‌های ناموفق زیاد باشد.

### تب ۳: 📈 تحلیل CGM
- آپلود اکسل قند (ستون زمان + ستون mg/dL).
- محاسبه: میانگین، GMI، TIR/TAR/TBR، CV، کمترین/بیشترین، نقاط بحرانی.
- نوار رنگی محدوده‌ها، هشدارهای هوشمند (ارجاع به پزشک)، لیست نقاط بحرانی.
- **نمودار AGP:** داده‌های همه‌ی روزها روی یک شبانه‌روز، با صدک‌های ۵/۲۵/۵۰/۷۵/۹۵ (SVG). توابع: `computeAGP` (سطل‌های ۳۰ دقیقه‌ای)، `agpSVG` (رسم)، `buildAGP`.
- **خروجی گزارش PDF:** `exportAGPReport` یک پنجره‌ی جدید با گزارش استاندارد AGP باز می‌کند: هدر، وضعیت حسگر، معیارها، Time in Ranges (نوار عمودی)، نمودار AGP، پروفایل روزانه (`dailyProfilesSVG` با تاریخ شمسی+میلادی)، و **جدول وعده‌های غذایی + انسولین + انسولین پایه** (`mealsInsulinReport` که از `planData` می‌خواند). کاربر با `window.print()` آن را PDF/چاپ می‌کند.
- داده‌ی قند در `window._cgmReadings`، آمار در `window._cgmStats`.

---

## ۴. یکپارچگی سه‌گانه (کامل شده)

جریان کامل داده که پیاده شده:
1. کاربر فایل **قند CGM** را در تب «تحلیل CGM» وارد می‌کند → `window._cgmReadings` پر می‌شود.
2. کاربر فایل **لاگ** را در برنامه‌ی هفتگی وارد می‌کند.
3. هنگام import هر وعده:
   - `matchNoteFoods()` نوت فارسی را می‌شکافد، غذاها را در دیتابیس پیدا می‌کند، کربوهیدرات تخمینی می‌زند.
   - `cgmStatsForMeal()` از `_cgmReadings` قند قبل/بعد/پیک/دره را حول زمان وعده پیدا می‌کند.
4. وعده با کربوهیدرات + قند کامل ثبت می‌شود → **کالیبراسیون I:C خودکار فعال می‌شود**.

**تست‌شده روی داده‌ی واقعی:** صبحانه‌ی ۲۶ تیر (تخم‌مرغ/نان/چای/هلو) → کرب ۳۶، قند قبل ۹۱، بعد ۱۲۰، پیک ۱۴۹ — با اپ CGM واقعی تطابق دارد.

---

## ۵. توابع کلیدی JavaScript (نقشه‌ی کد)

```
تقویم شمسی:      g2j, jalaliStr, jalaliShort, weekStart
ذخیره‌سازی:      storageGet/Set, saveProfile, loadProfile, savePlan, loadPlan
تب‌ها:           switchTab
دیتابیس/غذا:     DB (آبجکت), CAT_EMOJI, ITEM_EMOJI, itemEmoji, enrichUnits,
                 carbPerGram, unitFactor, MACRO_BY_CAT, pf
ماشین‌حساب:      addFoodRow, calcCarbs, calcDose, calcRatios, updateMacros,
                 renderFoodSummary, updateTiming, stepAmt, stepById
جستجو:           buildSearchIndex, normalizeFa, doSearch, doSearchPlan, pickSearch, pickPlanFood
برنامه هفتگی:    renderWeek, renderDay, saveMealEntry, renderSavedMeals, delMeal,
                 saveBasalDay, loadBasalDay, dayKey, dayKeySel
کالیبراسیون:     collectCalibMeals, updateCalibration, applyCalibIC
تحلیل CGM:       handleCGMFile, analyzeCGM, renderCGMResult
وارد کردن لاگ:   handleLogFile, parseLogTime, processLog, renderLogPreview,
                 toggleDayLog, selectAllLog, importSelectedLog
وصل CGM↔وعده:    cgmStatsForMeal
تشخیص غذا از نوت: matchNoteFoods, faNum
```

ساختار داده‌ی `planData`:
```js
planData = {
  "2026-07-26": [ {type,time,insType,insDose,carb,bgBefore,bgAfter,peak,trough,foods:[],id} ],
  "_basal": { "2026-07-26": {type,dose,time} }
}
```

---

## ۶. کلیدهای localStorage
- `insulin_profile_v1` — پروفایل
- `weekly_plan_v1` — برنامه‌ی هفتگی + انسولین پایه (`_basal`)

---

## ۷. فرمت فایل‌های ورودی (از اپ CGM کاربر)

**فایل قند (`CGM.xlsx`):** یک شیت، دو ستون: `Time` و `Sensor Reading(mg/dL)`. هر ۵ دقیقه. **دو فرمت تاریخ پشتیبانی می‌شود** (نسخه‌ی ۴.۳): با خط تیره `14-07-2026 20:46 GMT+3:30` و با اسلش `28/07/2026 18:57`. `parseLogTime` هر دو جداکننده‌ی `-` و `/` را می‌پذیرد. نام شیت مهم نیست چون اولین شیت خوانده می‌شود (`SheetNames[0]`). اگر اپ CGM فرمت دیگری صادر کرد، باید regex در `parseLogTime` گسترش یابد.

**فایل لاگ (`Logdata....xlsx`):** چند شیت:
- `Meal`: `Start Time, Meal, Food Intake, Total, Note` — نوت فارسی مثل «تخم مرغ ۳ عدد/ نان یک کف دست». ستون Total معمولاً `0g 0 Cal 0 P 0 F 0 C` (خالی — پس کربوهیدرات از نوت باید تخمین زده شود).
- `Insulin`: `Time, What type of insulin did you take?, Amount (units)` — نوع اغلب خالی است، فقط دوز.
- شیت‌های دیگر (Exercise, Medicine, Blood Glucose, Sleep, Mood) معمولاً خالی‌اند.

تطبیق وعده↔انسولین: بر اساس نزدیک‌ترین زمان (پنجره‌ی ۹۰ دقیقه) در `processLog`.

---

## ۸. کارهای باقی‌مانده / ایده‌های بعدی (برای Claude Code)

### ۸.۱ — نقشه‌ی مهاجرت به «سامانه‌ی کامل» (iOS + Android + وب با دیتابیس)

هدف کاربر: از این فایل تک‌صفحه‌ای به یک محصول چندسکویی با لاگین و دیتابیس ابری برسد. مسیر پیشنهادی:

**استک پیشنهادی:**
- **فرانت‌اند:** React (وب) + Capacitor یا React Native برای تبدیل به اپ iOS/Android از همان کد.
- **بک‌اند/دیتابیس/لاگین:** Supabase (پیشنهاد اول — Postgres + Auth + رایگان تا حد خوب) یا Firebase. این هم لاگین انحصاری می‌دهد، هم دیتابیس ابری که بین همه‌ی دستگاه‌ها sync می‌شود، هم ذخیره‌ی دائم.

**چرا این‌جا (چت) ساخته نشد:** سیستم لاگین واقعی به سرور و دیتابیس نیاز دارد؛ GitHub Pages فقط فایل ثابت پخش می‌کند. این مهاجرت باید در Claude Code انجام شود که بتوان پکیج نصب کرد، به Supabase وصل شد، کلید API گذاشت، و اپ موبایل بیلد کرد.

**ترتیب مهاجرت پیشنهادی در Claude Code:**
1. یک پروژه‌ی React بساز (Vite). منطق موجود (DB غذا، محاسبات، AGP، کالیبراسیون) را از این HTML به کامپوننت‌های React منتقل کن — این منطق مستقل از UI است و مستقیم قابل انتقال.
2. Supabase را وصل کن: جدول‌های `profiles`, `meals`, `basal`, `cgm_readings`, `cgm_files`, `bg_manual`, `exercise`. به‌جای localStorage از Supabase بخوان/بنویس.
3. Auth بساز (ایمیل/رمز یا OTP). هر کاربر فقط داده‌ی خودش را ببیند (Row Level Security).
4. با Capacitor خروجی iOS/Android بگیر.

**نکته‌ی مهم:** همه‌ی داده‌ها الان در localStorage با کلیدهای مشخص ذخیره می‌شوند (پایین را ببین). این‌ها نقشه‌ی مستقیم جدول‌های دیتابیس‌اند. تابع‌های `loadCGMFromStorage`, `loadBG`, `loadExercise`, `loadPlan`, `loadProfile` نقاط اتصال‌اند — فقط منبعشان از localStorage به Supabase عوض می‌شود.

### ۸.۲ — کلیدهای localStorage (نقشه‌ی جدول‌های دیتابیس آینده)
- `insulin_profile_v1` — پروفایل
- `weekly_plan_v1` — وعده‌ها + انسولین پایه (`_basal`)
- `cgm_data_v1` — داده‌ی خام قند (آرایه‌ی `{time, glu}`) — ادغام چند فایل، حذف تکراری بر اساس `time`
- `cgm_files_v1` — فهرست فایل‌های CGM واردشده (`{imported, from, to, count, added}`)
- `bg_manual_v1` — قند خون دستی (`{time, iso, glu}`)
- `exercise_v1` — ورزش (`{start, iso, what, dur}`)
- `agp_colors` — رنگ‌های دلخواه نمودار AGP
- `coll_state` — وضعیت باز/بسته‌ی کارت‌های کشویی
- `calib_filters` — فیلترهای کالیبراسیون I:C (`{lowCarb, stableBG, useMedian, rangeMode, rangeDays, rangeFrom, rangeTo}`)
- `cgm_cal_v1` — کالیبراسیون/افست CGM (`{offset, autoOffset, displayMode, trusted}`)

### ۸.۳ — بکاپ export/import (هنوز ساخته نشده — کار بعدی خوب)
تا وقتی دیتابیس ابری آماده نشده، یک دکمه که همه‌ی کلیدهای بالا را در یک فایل JSON واحد export و import کند تا داده بین دستگاه‌ها منتقل شود.

### ۸.۱۲ — کالیبراسیون/افست CGM (۴.۲)
CGM با گلوکومتر تفاوت دارد (تأخیر زمانی + خطای کالیبراسیون). روی داده‌ی واقعی: CGM به‌طور میانگین ۱۷ واحد **کمتر** از گلوکومتر است (میانه ۱۳)، و این تفاوت **بعد از غذا** بیشتر است (۲۴ در برابر ۵ ناشتا) به‌خاطر تأخیر CGM.
- `getCgmCal()`/`setCgmCal()` (کلید `cgm_cal_v1`: `{offset, autoOffset, displayMode, trusted}`).
- `collectCgmBgPairs()` — از `bg_manual` هر قند دستی را با نزدیک‌ترین CGM (تا ۱۵ دقیقه) جفت می‌کند و برچسب `afterMeal` می‌زند.
- `computeSuggestedOffset()` — میانگین/میانه‌ی (BG−CGM) + تفکیک بعد غذا/ناشتا + پراکندگی.
- `applyOffset(cgmVal)` — قند CGM + افست.
- `cgmStatsForMeal` وقتی `displayMode==='offset'` است، افست را به قند وعده اضافه می‌کند.
- `renderCgmCal()` کارت `c-cgmcal` را می‌سازد: راهنمای زمان اندازه‌گیری، جدول افست پیشنهادی، input افست قابل‌ویرایش، سه دکمه‌ی حالت نمایش (`cgm`/`offset`/`bg`)، جدول مقایسه‌ی سه‌گانه، و اعتمادسنجی (٪ تطابق با گلوکومتر تا ۱۰ واحد). صدا زده می‌شود در انتهای `runCGMAnalysis`.
- **هشدار صادقانه در UI:** وقتی پراکندگی > ۳۰، پیام می‌دهد که افست تقریب است نه عدد دقیق. کاربر خودش حالت نمایش را انتخاب می‌کند؛ پیش‌فرض `cgm` خام.
- **کار باقی‌مانده:** تغییر `displayMode` نیاز دارد `fillGlucoseFromCGM` دوباره اجرا شود تا قند وعده‌ها با حالت جدید به‌روز شود (فعلاً باید دستی فایل لاگ دوباره وارد شود یا دکمه‌ی پر کردن قند زده شود). می‌توان این را خودکار کرد.

### ۸.۱۳ — پر کردن خودکار قند بعد از افزودن سنسور (۴.۷)
مشکل رایج: کاربر لاگ را وارد می‌کند، بعد سنسور جدیدی اضافه می‌کند — ولی قند وعده‌های اخیر پر نمی‌شود چون پر کردن قند فقط هنگام import لاگ اتفاق می‌افتاد. `autoFillGlucoseIfNeeded()` بعد از هر import موفق CGM اجرا می‌شود: وعده‌هایی که `bgBefore`/`bgAfter` ندارند را پیدا می‌کند، با `cgmStatsForMeal` قندشان را از داده‌ی جدید پر می‌کند، و پیام سبز «قند N وعده خودکار پر شد» را بالای نتیجه‌ی CGM نشان می‌دهد. تست واقعی: قبل از سنسور دوم ۱۶ وعده قند داشتند، بعد از افزودن آن خودکار به ۴۲ رسید و I:C پنج‌روزه از «محاسبه نشد» به ۹.۳ رسید. دکمه‌ی دستی `fillGlucoseFromCGM` هم با توضیح واضح‌تر باقی است.

### ۸.۴ — سایر ایده‌ها
1. بهبود `matchNoteFoods` (تشخیص غذا از متن فارسی — خطاپذیرترین بخش). **مشکل شناخته‌شده:** وقتی مقدار غذا در متن نامشخص است، کمترین واحد (۱ قاشق/۱ عدد) فرض می‌شود که باعث کم‌تخمینی شدید کرب می‌شود (مثلاً «زرشک‌پلو» بدون مقدار → ۱ قاشق به‌جای یک بشقاب). راه‌حل پیشنهادی: پیش‌فرض منطقی‌تر برای هر دسته (پلو/برنج = وعده‌ی متوسط، نه ۱ قاشق) + علامت‌گذاری برای اصلاح دستی.
2. کالیبراسیون ISF از تجربه.
3. تطبیق نقاط بحرانی CGM با وعده‌ها و ورزش‌های همان بازه.
4. PWA/آفلاین کامل (inline کردن xlsx و فونت یا Service Worker).

### ۸.۵ — باکس «وعده‌های نیازمند بررسی» (پیاده‌سازی‌شده در ۳.۱، بهبود ۴.۵)
تابع `collectReviewMeals` سه نوع مشکل را تشخیص می‌دهد: (۱) انسولین دارد ولی غذا ثبت نشده، (۲) **وعده‌ی اصلی** (صبحانه/ناهار/شام) که غذا دارد ولی انسولین ندارد، (۳) وعده‌ی اصلی که فقط خوراکی سبک دارد (احتمالاً میان‌وعده‌ی اشتباه‌ثبت‌شده). **مهم (۴.۵):** میان‌وعده‌های بدون انسولین دیگر در بررسی نمی‌آیند — چون طبیعی است که میان‌وعده (گیلاس، بادام، چای) انسولین نداشته باشد. فقط وعده‌های اصلیِ بدون انسولین پرچم می‌خورند. `renderReviewMeals` آن‌ها را در کارت `c-review` با دکمه‌ی «اصلاح» و «نادیده بگیر» نشان می‌دهد.

### ۸.۶ — فرم ویرایش کامل + استثنا از کالیبراسیون (۳.۲)
`editMealForm` بازنویسی شد: جستجوی زنده‌ی دیتابیس (`doSearchEdit`/`pickEditFood`/`renderEditFoods`/`removeEditFood` با متغیر `window._editFoods`)، چیپس غذاهای قابل‌حذف، textarea دستی برای غذای خارج از دیتابیس، انتخاب نوع وعده، و چک‌باکس `excludeCalib`. وقتی کاربر این چک‌باکس را بزند، وعده در `collectCalibMeals` و `collectReviewMeals` نادیده گرفته می‌شود (فقط در تاریخچه می‌ماند). `saveEditMeal` همه را ذخیره می‌کند.

### ۸.۱۱ — فیلتر بازه‌ی زمانی + وعده‌های پرمصرف + پیست خوراکی (۳.۷–۴.۰)
**بازه‌ی زمانی (۳.۷، بازطراحی ۳.۹، بهبود ۴.۶):** در `calib_filters` فیلدهای `rangeMode` (`all`/`days`/`dates`)، `rangeDays` (عدد دلخواه)، `rangeFrom`/`rangeTo` (تاریخ شمسی `yyyy/mm/dd`). `setRangeMode` و `faToEn`. تابع `j2g(jy,jm,jd)`. **مهم (۴.۶):** «چند روز اخیر» بر اساس جدیدترین روزی که **هم وعده هم قند CGM** دارد حساب می‌شود (`latestWithCgm`)، نه جدیدترین وعده — وگرنه اگر وعده‌های اخیر قند نداشته باشند (سنسور قدیمی‌تر از وعده‌هاست)، بازه به روزهای بدون‌قند اشاره می‌کند و هیچ وعده‌ای موفق نمی‌شود. برای حالت `days` هم `rangeEnd` تنظیم می‌شود تا سقف بازه هم محدود شود. روی داده‌ی واقعی: ۵ روز اخیر (منتهی به آخرین قند) ۷.۸، ۷ روز ۶.۸.

**وعده‌های پرمصرف با تطبیق جزئی (۳.۸، بهبود ۴.۰):** `collectFrequentMeals` حالا وعده‌ها را بر اساس **غذاهای مشترک** گروه‌بندی می‌کند (نه ترکیب دقیق). دو وعده هم‌گروه می‌شوند اگر حداقل نصفِ غذاهای وعده‌ی کوچک‌تر مشترک باشد. نماینده‌ی گروه = کامل‌ترین وعده. مثال: «برنج+مرغ+ماست»، «برنج+مرغ»، «برنج+مرغ+سالاد» → یک گروه ۳×. کاربر یکی را انتخاب و سپس ویرایش می‌کند.

**در ماشین‌حساب (۴.۰):** `toggleFrequentPanelCalc`/`loadFrequentMealCalc` و `togglePastePanelCalc`/`pasteCalcFoods` نسخه‌ی ماشین‌حسابی‌اند که غذا را به `foodRows` اضافه می‌کنند (`addFoodToCalcRows`). هم لود پرمصرف هم پیست از `matchNoteFoods` برای تطبیق دقیق استفاده می‌کنند (نه تطبیق نام خام). دکمه‌ها زیر «افزودن خوراکی» در کارت محاسبه‌ی کربوهیدرات.

### ۸.۱۰ — پنل «فیلترهای مفید» کالیبراسیون (۳.۶)
سیستم یکپارچه‌ی فیلتر با `getCalibFilters()`/`setCalibFilter(key,val)` (کلید localStorage `calib_filters`، پیش‌فرض `{lowCarb:true, stableBG:false, useMedian:false, dayRange:0}`). فیلترها:
- **lowCarb** — حذف وعده‌های کرب < ۲۰ گرم (انسولینشان معمولاً برای کرب نبوده). `getLowCarbCut()` حالا از این می‌خواند.
- **stableBG** — فقط وعده‌هایی که |bgAfter − bgBefore| ≤ ۵۰ (I:C درست قند را ثابت نگه می‌دارد؛ وعده‌های نامتناسب کنار می‌روند).
- **useMedian** — میانه‌ی نسبت‌ها به‌جای میانگین (مقاوم در برابر پرت‌ها).

هر وعده در `collectCalibMeals` با فلگ‌های `lowCarb`/`unstable` علامت می‌خورد و `good=false` می‌شود. محاسبه‌ی `realIC` بسته به `useMedian` میانگین یا میانه است. پنل UI سه چک‌باکس با توضیح و شمارش وعده‌های کنارگذاشته‌شده دارد. روی داده‌ی واقعی: بدون فیلتر ۵.۳ (۲۰ وعده)، فقط کم‌کرب ۶.۳ (۱۶)، فقط میانه ۶.۰، هر سه ۶.۵ (۱۳) — همه در بازه‌ی ۵.۳–۶.۵ (فیلترها عدد را تثبیت می‌کنند نه دلخواه). متن پنل کاربر را به مشورت با پزشک درباره‌ی انتخاب فیلتر ارجاع می‌دهد.

---

## ۹. نکات فنی مهم

- **باگ تاریخ (حل‌شده):** `dayKey()` باید از تاریخ **محلی** استفاده کند نه `toISOString()` (که UTC است و در ایران GMT+3:30 باعث می‌شد وعده‌های بامداد به روز قبل بروند). این حل شده — اگر تابع را تغییر دادی، این نکته را رعایت کن.
- **روز پیش‌فرض:** ورود به تب برنامه با `goToToday()` روی امروز تنظیم می‌شود (نه شنبه).
- **ضدتکرار import:** `importSelectedLog` وعده‌های با تاریخ+ساعت+نوع یکسان را رد می‌کند.

- **تست:** برای تست محلی xlsx (چون CDN در محیط تست بلاک است)، `npm pack xlsx@0.18.5` و جایگزینی موقت مسیر CDN با فایل محلی. در نسخه‌ی نهایی حتماً CDN بماند.
- **کش گیت‌هاب:** کاربر روی GitHub Pages میزبانی می‌کند (`macansanat.github.io/calansolin` — الان private شده). متاتگ‌های ضدکش اضافه شده، ولی برای دیدن نسخه‌ی جدید از `?v=N` استفاده شود.
- **موبایل:** ریسپانسیو تست‌شده روی عرض ۳۹۰px (آیفون). select‌ها full-width، استپرها، بدون overflow افقی. کاربر روی آیفون با Safari باز می‌کند.
- **زبان:** کل UI فارسی محاوره‌ای. کاربر فارسی‌زبان ایرانی است.
- **ذخیره‌سازی حساس:** فایل‌های CGM فقط در مرورگر پردازش می‌شوند، جایی ارسال نمی‌شوند (به کاربر هم گفته شده).

---

## ۱۰. یافته‌های بالینی مهم (زمینه)

از تحلیل داده‌های واقعی کاربر:
- قاعده‌ی ۵۰۰ برای این فرد I:C=۲۷.۸ داد، ولی **I:C واقعی از تجربه ≈ ۹** بود (چون دیابت نوع ۲ با مقاومت انسولین — قاعده‌ی عمومی برایش کار نمی‌کند). این دقیقاً چرا کالیبراسیون از روی تجربه ساخته شد.
- TIR ~۸۸٪، GMI ~۶.۳٪، کنترل کلی خوب. نقاط افت پراکنده (تا ۵۴) و چند اوج شبانه دیده شد.
- این یافته‌ها **نمونه‌ی زنده‌ی چرایی خط قرمز** است: نرم‌افزار با قاعده‌ی عمومی عدد غلط (۱.۶ واحد) می‌داد، ولی تجربه‌ی واقعی (۶ واحد) درست بود. برنامه نباید تجویز کند — باید تجربه را منعکس کند.

---

## ۱۱. لاگ تغییرات نسخه‌ها

### نسخه ۶.۴ (این چت)
- **صدای «دینگ» عمومی روی دکمه‌های UI هر دو تب:** تابع جدید `uiDing()` — یک دینگ کوتاهِ بالارونده (۶۶۰→۸۸۰Hz) با Web Audio. **نکته‌ی طراحی:** این تابع عمداً **مستقل از `_wkSound`** است (برخلاف `wkBeep` که با `if(!_wkSound) return` شروع می‌شود)، چون این صدای فیدبکِ UI عمومی است نه صدای کرنومتر؛ نباید با خاموش‌کردن صدای تمرین قطع شود. وصل شد به: دکمه‌های انتخاب هدف ورزش (`showExPlan` در `renderExPlan`)، دکمه‌ی «انتخاب» بسته‌های آزمایش (`selectLabPackage` در `renderLabPackages`)، دکمه‌ی «افزودن» حرکت، و دکمه‌های «برنامه‌ی آماده» کرنومتر (`wkLoadPlan`). الگو: `onclick="uiDing();origFn()"`.
- **عمداً وصل نشد:** دکمه‌های 🔊/🎵 (خودشان تاگل صدا/موزیک‌اند، دینگ اضافه گیج‌کننده است). اگر بعداً خواستی دکمه‌ی دیگری دینگ بگیرد، فقط `uiDing();` را جلوی onclick اضافه کن.
- **نسخه:** فوتر ۶.۴.

### نسخه ۶.۳ (این چت)
- **صدای کلیک روی دکمه‌های ست/ثانیه:** همان موتور صدای کرنومتر (`wkBeep` با Web Audio، آفلاین) حالا به دکمه‌های ± در «شروع تمرین» هم وصل شد. دو تابع جدید: `wkClickUp()` (نت ۷۴۰Hz برای افزایش) و `wkClickDown()` (۵۶۰Hz برای کاهش). در `wkSets`/`wkDur` فقط وقتی مقدار **واقعاً تغییر کرد** صدا پخش می‌شود (نه وقتی به سقف/کف رسیده). صداها تابع `_wkSound`اند؛ با 🔇 خاموش می‌شوند. **iOS:** به `wkBeep` یک `ctx.resume()` برای حالت suspended اضافه شد تا بعد از قفل صفحه هم صدا برگردد.
- **اسپینر چرخشی عدد (کامپوننت جدید، مثل time picker موبایل):** با کلیک روی **عددِ** ست یا ثانیه (نه دکمه‌ها) یک چرخ اسکرولی باز می‌شود که با کشیدن انگشت عدد را سریع انتخاب می‌کنی؛ هر تغییر یک تیک صدا می‌دهد. دکمه‌های ± هم سر جایشان ماندند (هر دو راه فعال). توابع: `openSpinner(title,values,current,fmt,onPick)` (عمومی)، `spinPickIdx`, `spinConfirm`, `closeSpinner`، و دو فراخوان مخصوص: `wkSetsSpin(i)` (۱ تا ۱۲ ست) و `wkDurSpin(i)` (۱۰ تا ۱۸۰۰ ثانیه، پله‌ی ۵). **مقاوم‌سازی:** اگر مقدار فعلی دقیقاً در لیست نبود (مثلاً ثانیه‌ی غیرمضرب ۵ از `parseSetInfo`)، اسپینر به **نزدیک‌ترین** مقدار می‌رود نه اول لیست. مکانیزم: اسکرول‌اسنپ CSS (هر آیتم ۳۶px، `scroll-snap-align:center`، دو `spin-pad` بالا/پایین ۷۲px برای وسط‌چین)، `scrollTop=idx*36`. CSS با پیشوند `spin-`.
  - **الگوی قابل‌تکرار:** همین `openSpinner` عمومی است؛ برای هر انتخاب عددی دیگر (مثلاً استراحت، یا دوز در جای دیگر) می‌شود دوباره استفاده کرد. کافی است آرایه‌ی مقادیر + `onPick` بدهی.
- **نسخه:** فوتر ۶.۳.

### نسخه ۶.۲ (چت قبل‌تر)
- **رفع همپوشانی ساعت/وعده در فرم ثبت ورزش:** در `renderExLogEntry` لیبل‌های «ساعت» و «وعده/نوبت» روی فیلد کناری می‌افتادند. علت: در این پروژه `label` به‌صورت پیش‌فرض `display:block` با margin است (خط ~۴۷ CSS)، و لیبل‌های داخل flex بدون `display:block;margin:0` درست نمی‌نشستند. اصلاح شد با `display:block;margin:0 0 6px` روی همه‌ی لیبل‌های فیلد + ساختار flex محکم‌تر (`min-width:0`). **درس برای فیلدهای جدید:** هر لیبلی که داخل یک ستون flex می‌گذاری باید `display:block` صریح داشته باشد.
- **خوانایی بخش «شروع تمرین (با کرنومتر)» (`renderWkList`/`renderExWorkout`):** این بخش در ۶.۱ جا مانده بود. حالا مثل تب آزمایش خوانا شد: نام حرکت ۱۳→۱۶px، جزئیات ست/زمان ۱۰.۵→۱۲.۵px، متن راهنما و دکمه‌های «برنامه‌ی آماده» بزرگ‌تر. کلاس‌های جدید CSS: `wk-row-name`, `wk-row-meta`, `wk-move-btn` (▲▼ بزرگ‌تر ۳۴×۲۸)، `wk-del-btn` (✕ حالا دکمه‌ی مربعی قرمز واضح ۳۶×۳۶ به‌جای آیکون ریز)، `wk-ctrl-label`, `wk-step` + `wk-step button` (استپرهای ±  از ۲۶×۲۶ به ۳۸×۳۸ با فونت ۲۰px)، `wk-step-val`. **نکته:** استایل‌ها به‌صورت کلاس CSS درآمدند تا تکرار inline کم شود؛ ردیف حالت اجرا (کرنومتر فعال) دست‌نخورده ماند.
- **نسخه:** فوتر ۶.۲.

### نسخه ۶.۱ (چت قبل‌تر)
- **رفع باگ `undefined/undefined` در تاریخ:** تابع `g2j` یک **آرایه** `[jy,jm,jd]` برمی‌گرداند، ولی چند جا به‌اشتباه به‌صورت آبجکت (`jT.jy`, `jToday.jm`, ...) خوانده می‌شد که `undefined` می‌داد. سه نقطه اصلاح شد: `renderLabEntry` (تاریخ آزمایش)، `renderExLogEntry` (تاریخ ثبت ورزش)، و `wkFinish` (ثبت خودکار بعد از کرنومتر). **درس:** همیشه خروجی `g2j`/`j2g` را با اندیس آرایه‌ای (`[0]/[1]/[2]`) بخوان، نه `.jy/.jm/.jd`.
- **تقویم شمسی پاپ‌آپ مشترک (کامپوننت جدید):** به‌جای input متنی که کاربر باید دستی تاریخ تایپ کند، یک تقویم کامل اضافه شد. توابع کلیدی: `openJCal(inputId,onPick)`، `renderJCal`، `jcalMonth(dir)` (ناوبری ماه)، `jcalSet(field,val)` (dropdown ماه/سال)، `pickJCalDay(d)`، `jcalToday`/`jcalSetTodayField` (دکمه‌ی امروز)، `commitJCal`، `closeJCal`. کمکی‌ها: `jMonthLen(jy,jm)` (طول ماه شمسی با تشخیص کبیسه از روی رفت‌وبرگشت `j2g`→`g2j`)، `jFirstDow(jy,jm)` (روز هفته‌ی اول ماه، شنبه=۰)، `parseJDate(s)` (خواندن رشته با ارقام فارسی/انگلیسی)، `jDateStr`، `jDateFieldHTML(inputId,initialStr)` (سازنده‌ی فیلد + دکمه‌ی امروز). CSS با پیشوند `jcal-`/`jdate-`. **برای استفاده در هر فیلد تاریخ جدید فقط `jDateFieldHTML('someId', jDateStr(...))` را در HTML بگذار.** تست‌شده با Node: طول ماه‌ها، کبیسه‌ی ۱۴۰۳ (اسفند ۳۰) در برابر ۱۴۰۴ (۲۹)، رفت‌وبرگشت، و parse ارقام فارسی — همه درست.
- **خوانایی ردیف‌های آزمایش:** ردیف‌های `renderLabEntry` بازطراحی شد. کلاس‌های جدید `lab-row`/`lab-info`/`lab-fa`/`lab-en`/`lab-val`/`lab-del`/`lab-cat-head`. فونت نام فارسی از ۱۲px به ۱۵px، ورودی مقدار بزرگ‌تر (۱۶px, `inputmode=decimal`)، و دکمه‌ی حذف (✕) از یک آیکون کوچک بی‌جا به یک دکمه‌ی مربعی واضح با پس‌زمینه‌ی قرمز کم‌رنگ در سمت مناسب تبدیل شد.
- **تب ورزش — ساعت و وعده:** به فرم ثبت ورزش (`renderExLogEntry`) دو فیلد اضافه شد: `exTime` (ساعت، `input type=time` با پیش‌فرض الان) و `exSlot` (وعده/نوبت: صبح/بعد صبحانه/ظهر/... یا بدون نوبت). `saveExToday` حالا `time` و `slot` را هم ذخیره می‌کند، `wkFinish` هم `time` را خودکار می‌زند. `renderExHistory` ساعت و وعده را کنار تاریخ نمایش می‌دهد. چون هر ثبت `ts` مجزا دارد، **چند جلسه در یک روز** بدون مشکل ثبت می‌شود.
- **نسخه:** فوتر به ۶.۱ به‌روز شد. (یادآوری: اگر کاربر گفت نسخه عوض نشد، اول **کش مرورگر** را با `?v=` چک کن — بخش زیر.)

### نسخه ۶.۰ (چت قبل)
- **حذف نوار `safety` بالای تب‌ها:** بلوک `<div class="safety">` که زیر header و بالای همه‌ی تب‌ها بود (هشدار ایمنی کوتاه) حذف شد. header (عنوان + زیرعنوان «ابزار آموزشی برای یادگیری — نه جایگزین پزشک») باقی ماند.
- **رفع نمایش دو کارت هشدار در همه‌ی تب‌ها:** کارت‌های `disclaimer` («محدودیت‌های مهم») و `final-warn` («این یک نرم‌افزار آموزشی است...») به‌اشتباه **بیرون** از همه‌ی `page`ها (بین بسته‌شدن `page-cgm` و باز شدن `page-labs`) قرار داشتند، برای همین در تب‌های ورزش و آزمایش و بقیه هم دیده می‌شدند. این دو کارت به **انتهای تب ماشین‌حساب** (`page-calc`، قبل از بسته‌شدنش) منتقل شدند تا فقط آنجا نمایش داده شوند. یک `</div>` معلق هم که همراهشان بود تصحیح شد. ساختار div با پارسر تأیید شد (۰ باز مانده، ۰ بی‌جفت).
- **خط قرمز ایمنی حفظ شد:** متن کارت قرمز و disclaimer دست‌نخورده ماند؛ فقط جایگاهشان به تب ماشین‌حساب محدود شد. هسته‌ی ایمنی پابرجاست.
- **نسخه:** نشانگر نسخه در فوتر از ۵.۹ به ۶.۰ به‌روز شد (خط حدود ۸۵۴، `نسخه‌ی <b>۶.۰</b>`).

> **⚠️ نکته‌ی مهم برای چت بعدی — «فوتر/نسخه آپدیت نشد»:** اگر کاربر گفت «نسخه یا زمان بارگذاری توی فوتر عوض نشد»، این **باگ کد نیست**. کد نسخه و تابع `loadStamp` (خط ~۸۵۳–۸۶۱) درست کار می‌کنند. علت **کش مرورگر/GitHub Pages** است (Safari آیفون نسخه‌ی قدیمی را نگه می‌دارد). راه‌حلی که باید به کاربر گفت: آدرس را با پارامتر نسخه‌ی جدید باز کند، مثل `...insulin-calculator.html?v=60` و هر بار عدد را عوض کند (`?v=61`، `?v=62`، ...). **قبل از هر تغییر کد، اول این را چک کن** — وگرنه دنبال باگی می‌گردی که وجود ندارد. (این همان نکته‌ی بخش ۹ «کش گیت‌هاب» است، اینجا هم تکرار شد چون زیاد پیش می‌آید.)

---

*آخرین وضعیت: هر سه تب کامل، یکپارچگی سه‌گانه پیاده و تست‌شده. نسخه ۶.۴: صدای دینگ عمومی (`uiDing`) به دکمه‌های انتخاب هدف ورزش، بسته‌های آزمایش، افزودن حرکت و برنامه‌های آماده وصل شد. آماده برای ادامه در Claude Code.*
